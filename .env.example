# 이 파일을 복사해서 .env 파일을 만든 뒤 실제 값으로 채우세요.
# cp .env.example .env

# OpenAI API 키 (필수). https://platform.openai.com/api-keys 에서 발급
OPENAI_API_KEY=

# 사용할 모델 (선택). 기본값 gpt-5.5
OPENAI_MODEL=gpt-5.5

# 로컬 서버 주소 (선택)
HOST=127.0.0.1
PORT=4173
